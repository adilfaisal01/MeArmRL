# Copy to docker/.env and edit, or set inline:
#   TASK=Mearmrl-Reach-v0 NUM_ENVS=512 docker compose up
#
# These configure the `mearmrl` training service (docker compose up).
# Defaults match the week-01 lab budget (student task).

TASK=Mearmrl-Reach-Student-v0
NUM_ENVS=1024
SEED=42
MAX_ITERATIONS=4800
